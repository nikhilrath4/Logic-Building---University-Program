#Program 2: Remove Last Digit

number = int(input("Enter the number: "))

remove = number // 10 #division int doesn't show the digits after decimal

print("After removing the last digit we get", remove)
